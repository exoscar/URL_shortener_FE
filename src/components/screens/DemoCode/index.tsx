import React from "react";
import { Container } from "./style";
import { Typography } from "@mui/material";

const DemoPage = () => {
  return (
    <Container>
      <Typography className="some-text">Apples are Sweet</Typography>
    </Container>
  );
};

export default DemoPage;
